/**
 * Saved Products (Wishlists) & Recently Viewed (Phase 4 · M20) — shared constants.
 * Own-account customer convenience; never reserves stock or moves money.
 */

/** Newest-N cap for a user's recently-viewed history (older rows are pruned). */
export const RECENTLY_VIEWED_MAX = 50;

/** Page size for the saved-products (wishlist) listing. */
export const SAVED_PRODUCTS_PAGE_SIZE = 24;
